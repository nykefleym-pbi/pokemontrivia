// Bare config so vite-node can run the simulator without loading the app's
// Nitro/TanStack plugin stack (which needs a real dev server).
import { defineConfig } from "vite";

export default defineConfig({});
